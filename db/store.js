const util = require('util')
const fs = require('fs');

const uuidv1 = require('uuid/v1')

const readFileSync
const writeFileSync

getNotes() {
    return this.read().then((notes))
    let parsedNote
}

addNote(note)
const { title, text} 